# ═══════════════════════════════════════════════════════════════════════════
# Splitwise Clone - Environment Variables
# ═══════════════════════════════════════════════════════════════════════════
# Copy this file to .env and update the values for your environment
# The .env file should NEVER be committed to version control

# ───────────────────────────────────────────────────────────────────────────
# Database Configuration
# ───────────────────────────────────────────────────────────────────────────

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
# Examples:
#   Local: postgresql://postgres:password@localhost:5432/splitwise_db
#   Neon: postgresql://user:pass@ep-example.us-east-2.aws.neon.tech/neondb?sslmode=require
#   Heroku: postgresql://user:pass@ec2-host.compute-1.amazonaws.com:5432/dbname
DATABASE_URL=postgresql://postgres:password@localhost:5432/splitwise_db

# Enable SQL query logging (useful for debugging, disable in production)
# Options: true, false
DB_ECHO=false

# Enable SSL for database connections (required for most cloud databases like Neon, Heroku)
# Options: true, false
DB_SSL=true

# ───────────────────────────────────────────────────────────────────────────
# JWT Authentication Configuration
# ───────────────────────────────────────────────────────────────────────────

# Secret key for signing JWT tokens
# IMPORTANT: Generate a strong random key for production!
# You can generate one with: openssl rand -hex 32
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-use-openssl-rand-hex-32

# JWT signing algorithm (HS256 is recommended for symmetric key signing)
JWT_ALGORITHM=HS256

# Token expiration time in minutes
# Default: 1440 minutes = 24 hours
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ───────────────────────────────────────────────────────────────────────────
# Application Configuration
# ───────────────────────────────────────────────────────────────────────────

# Application name (shown in API docs)
APP_NAME=Expense Splitter API

# Application version
APP_VERSION=1.0.0

# Application description (shown in API docs)
APP_DESCRIPTION=A FastAPI backend for splitting expenses among groups of users.

# Environment: development, staging, production
ENVIRONMENT=development

# Enable debug mode (provides detailed error messages)
# IMPORTANT: Set to false in production!
# Options: true, false
DEBUG=true

# ───────────────────────────────────────────────────────────────────────────
# Server Configuration
# ───────────────────────────────────────────────────────────────────────────

# Server host (0.0.0.0 allows external access, 127.0.0.1 is localhost only)
SERVER_HOST=0.0.0.0

# Server port
SERVER_PORT=8000

# Enable auto-reload on code changes (useful for development)
# IMPORTANT: Set to false in production!
# Options: true, false
SERVER_RELOAD=true

# ───────────────────────────────────────────────────────────────────────────
# CORS (Cross-Origin Resource Sharing) Configuration
# ───────────────────────────────────────────────────────────────────────────

# Allowed origins for CORS (comma-separated list)
# Use "*" to allow all origins (NOT recommended for production)
# Examples:
#   Development: *
#   Production: https://app.example.com,https://www.example.com
#   Multiple: http://localhost:3000,http://localhost:5173,https://app.example.com
CORS_ORIGINS=*

# Allow credentials (cookies, authorization headers) in CORS requests
# Options: true, false
CORS_CREDENTIALS=true

# Allowed HTTP methods (comma-separated list)
# Use "*" to allow all methods
# Examples: GET,POST,PUT,DELETE,PATCH
CORS_METHODS=*

# Allowed HTTP headers (comma-separated list)
# Use "*" to allow all headers
# Examples: Content-Type,Authorization,X-Requested-With
CORS_HEADERS=*

# ═══════════════════════════════════════════════════════════════════════════
# PRODUCTION CONFIGURATION CHECKLIST
# ═══════════════════════════════════════════════════════════════════════════
#
# Before deploying to production, make sure to:
#
# ✅ Set a strong, random JWT_SECRET_KEY (use: openssl rand -hex 32)
# ✅ Set DATABASE_URL to your production database
# ✅ Set DEBUG=false
# ✅ Set SERVER_RELOAD=false
# ✅ Set DB_ECHO=false
# ✅ Configure CORS_ORIGINS with specific allowed domains (not "*")
# ✅ Use HTTPS for all production URLs
# ✅ Enable database backups
# ✅ Set up proper logging and monitoring
# ✅ Review and test security settings
#
# ═══════════════════════════════════════════════════════════════════════════
# ENVIRONMENT-SPECIFIC EXAMPLES
# ═══════════════════════════════════════════════════════════════════════════
#
# ── Development (Local) ──────────────────────────────────────────────────
# DATABASE_URL=postgresql://postgres:password@localhost:5432/splitwise_dev
# DB_SSL=false
# DEBUG=true
# SERVER_RELOAD=true
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# ENVIRONMENT=development
#
# ── Staging ──────────────────────────────────────────────────────────────
# DATABASE_URL=postgresql://user:pass@staging-db.example.com:5432/splitwise_staging
# DB_SSL=true
# DEBUG=false
# SERVER_RELOAD=false
# CORS_ORIGINS=https://staging.example.com
# ENVIRONMENT=staging
#
# ── Production ───────────────────────────────────────────────────────────
# DATABASE_URL=postgresql://user:pass@prod-db.example.com:5432/splitwise_prod
# DB_SSL=true
# DEBUG=false
# SERVER_RELOAD=false
# CORS_ORIGINS=https://app.example.com,https://www.example.com
# ENVIRONMENT=production
# JWT_ACCESS_TOKEN_EXPIRE_MINUTES=720  # 12 hours for production
#
# ═══════════════════════════════════════════════════════════════════════════
